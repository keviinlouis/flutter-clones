import 'package:flutter/material.dart';

class TextWithHashTag extends StatelessWidget {
  final String text;

  const TextWithHashTag({Key key, this.text}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
